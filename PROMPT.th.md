# ใช้ thai-docx ในแอปแชตที่ไม่มีเมนูสกิล

หน้านี้สำหรับคนที่ใช้แอปแชต AI ที่ **ไม่มีเมนูติดตั้งสกิล** แต่ **รันโปรแกรม Python กับไฟล์ที่แนบได้**
วิธีใช้คือส่งไฟล์สกิลให้แอปพร้อมข้อความชุดหนึ่ง แล้วแอปจะใช้โปรแกรมของสกิลสร้างไฟล์ Word ภาษาไทยให้

[English version](PROMPT.md) · [คู่มือการใช้งาน](https://github.com/sayam/thai-docx-skill/blob/main/docs/guide/th.md)

## ทำงานอย่างไร

เทียบได้กับการจ้างช่างมาทำงาน แล้วยื่นกล่องเครื่องมือกับใบสั่งงานให้

- **กล่องเครื่องมือ** คือไฟล์ `thai-docx-<เลขรุ่น>.zip` ข้างในมีโปรแกรมที่สร้างไฟล์ Word ภาษาไทยได้ถูกต้อง
- **ใบสั่งงาน** คือข้อความในกรอบด้านล่าง บอกแอปว่าต้องใช้เครื่องมือในกล่องนี้ ห้ามทำเอง

ไฟล์ Word ที่ได้เหมือนกับที่สกิลสร้างทุกไบต์ เพราะใช้โปรแกรมตัวเดียวกัน

## ก่อนเริ่ม

- แอปแชตต้อง **แนบไฟล์ zip ได้** และ **รันโปรแกรม Python ได้**
  (บางแอปเรียกว่า "วิเคราะห์ข้อมูล" "รันโค้ด" หรือ "Code execution" และบางแอปต้องเปิดในหน้าตั้งค่าก่อน)
- ถ้าแอปมี **เมนูสกิล** อยู่แล้ว เช่น แอป Claude หรือ ChatGPT บางแพ็กเกจ ให้ติดตั้งสกิลแทน
  จะใช้ง่ายกว่า ดู[ติดตั้งและเริ่มใช้](https://github.com/sayam/thai-docx-skill/blob/main/docs/guide/th/install.md)
- วิธีนี้ **ยังไม่ได้ลองกับแอปใดเลย** ถ้าลองแล้วได้หรือไม่ได้ ช่วยเล่าที่หน้า
  [Issues](https://github.com/sayam/thai-docx-skill/issues) ว่าใช้แอปอะไร และเจออะไร

## ขั้นที่ 1: ดาวน์โหลดไฟล์สกิล

1. เปิดหน้า [Releases](https://github.com/sayam/thai-docx-skill/releases/latest)
2. เลื่อนลงไปที่หัวข้อ **Assets**
3. กดชื่อไฟล์ `thai-docx-<เลขรุ่น>.zip` เช่น `thai-docx-0.2.2.zip`
   (อย่ากด "Source code" ไฟล์นั้นไม่ใช่ไฟล์ที่ต้องใช้)
4. **ไม่ต้องแตกไฟล์** เก็บไว้เป็นไฟล์ zip แบบนั้น

## ขั้นที่ 2: เปิดแชตใหม่และแนบไฟล์

1. เปิดแอปแชต แล้วเริ่ม **แชตใหม่**
2. กดปุ่มแนบไฟล์ (มักเป็นรูปคลิปหนีบกระดาษ 📎 หรือเครื่องหมาย **+**)
3. เลือกไฟล์ `thai-docx-<เลขรุ่น>.zip` ที่ดาวน์โหลดไว้
4. หากมีไฟล์ของตนเองที่จะใช้ด้วย เช่น ไฟล์ Markdown รูปภาพ หรือไฟล์โปรไฟล์ `.json` ให้แนบไปพร้อมกัน
5. **ยังไม่ต้องกดส่ง**

## ขั้นที่ 3: วางใบสั่งงาน แล้วพิมพ์คำขอต่อท้าย

1. ในกรอบด้านล่าง กดปุ่ม **คัดลอก** ที่มุมขวาบนของกรอบ
2. กลับไปที่ช่องพิมพ์ข้อความในแชต แล้ว **วาง** (กด Ctrl+V หรือแตะค้างแล้วเลือก "วาง")
3. ขึ้นบรรทัดใหม่ต่อท้าย แล้วพิมพ์สิ่งที่อยากได้ เช่น
   `ช่วยทำไฟล์ Word เรื่องวิธีประหยัดน้ำในบ้านแบบสั้น ๆ`
4. กด **ส่ง**

```text
ฉันแนบไฟล์ thai-docx-<เลขรุ่น>.zip มากับข้อความนี้ ข้างในมีโปรแกรมที่สร้างไฟล์ Word ภาษาไทยได้ถูกต้อง
ทุกครั้งที่ฉันขอไฟล์ Word (.docx) ที่มีภาษาไทย ให้ทำตามกฎต่อไปนี้ แทนการเขียนโค้ดสร้างเอกสารเอง

1. แตกไฟล์ zip ครั้งเดียว แล้วอ่านไฟล์ thai-docx/SKILL.md และทำตามที่เขียนไว้ ไฟล์นั้นเขียนไว้สำหรับ AI
   คำว่า <skill> ในไฟล์นั้น หมายถึงโฟลเดอร์ thai-docx ที่แตกออกมา
2. เขียนเนื้อหาเป็นไฟล์ Markdown แล้วรันคำสั่ง
   python3 <skill>/scripts/thai_docx build doc.md doc.docx
   แล้วอ่านข้อความ JSON ที่คำสั่งพิมพ์ออกมา
   ห้ามใช้ python-docx หรือไลบรารีอื่น และห้ามอ่านโค้ดของโปรแกรม
3. ห้ามเว้นวรรคระหว่างคำภาษาไทย ห้ามใส่ตัวอักษรที่มองไม่เห็น และห้ามแก้ถ้อยคำของฉันเพื่อให้สร้างไฟล์ผ่าน
4. ส่งไฟล์ .docx ให้ฉันดาวน์โหลด แล้วบอกเป็นภาษาของฉันสัก 2-3 บรรทัดว่าใช้การตั้งค่าอะไร และบอกว่าเปลี่ยนการตั้งค่าได้
5. อย่าถามเรื่องฟอนต์หรือรูปแบบหน้า เว้นแต่ข้อความของฉันมีคำว่า thai-docx grill
```

## ขั้นที่ 4: รอ แล้วดาวน์โหลดไฟล์

1. แอปจะแตกไฟล์ อ่านวิธีใช้ เขียนเนื้อหา แล้วรันโปรแกรม อาจใช้เวลาสักครู่
2. แอปจะตอบกลับพร้อมไฟล์ เช่น `water.docx` และบอกการตั้งค่า เช่น

   > สร้าง `water.docx` แล้ว — ฟอนต์ TH Sarabun New 16 pt, กระดาษ A4, ขอบซ้าย 1.5 นิ้ว
   > ด้านอื่น 1 นิ้ว ปรับค่าใดก็ได้

3. กดที่ชื่อไฟล์หรือปุ่มดาวน์โหลด แล้วเปิดด้วย Word

## ขั้นที่ 5: ขอแก้หรือขอไฟล์ใหม่ ในแชตเดิม

พิมพ์ต่อในแชตเดิมได้เลย ไม่ต้องแนบ zip หรือวางใบสั่งงานซ้ำ เช่น

- `ขอฟอนต์ Sarabun ขนาด 14 และใส่เลขหน้ากลางล่าง`
- `ใส่สารบัญด้วย`
- `thai-docx grill ช่วยทำรายงานการประชุม` (ถ้าอยากให้แอปถามการตั้งค่าก่อน)

สิ่งที่ขอได้ทั้งหมด ดูที่[สถานการณ์การใช้งาน](https://github.com/sayam/thai-docx-skill/blob/main/docs/guide/th/scenarios.md)

**ถ้าเริ่มแชตใหม่** ต้องทำขั้นที่ 2 และ 3 ใหม่ทุกครั้ง เพราะแอปจำไฟล์ข้ามแชตไม่ได้

## ข้อควรรู้

- **โปรไฟล์ไม่ถูกเก็บให้** ถ้าบันทึกการตั้งค่าเป็นโปรไฟล์ แอปจะให้ไฟล์ `.json` มา ให้เก็บไฟล์นั้นไว้เอง
  แล้วแนบมาพร้อม zip ในแชตครั้งหน้า
- **รูปภาพต้องแนบในแชต** และต้องเป็น PNG หรือ JPEG
- **ไฟล์ Markdown ที่มีอยู่แล้ว** แนบมาได้ แอปจะใช้ตามที่เขียนไว้ทุกตัวอักษร
- **ต้องเปิดไฟล์ด้วยเครื่องที่มีฟอนต์** TH Sarabun New ถ้าไม่มี ให้ขอฟอนต์ที่เครื่องมี

## ถ้ามีปัญหา

| เจออะไร | ทำอย่างไร |
|---|---|
| แอปบอกว่าแนบไฟล์ zip ไม่ได้ | แอปนี้ใช้วิธีนี้ไม่ได้ ลองแอปที่มีเมนูสกิล หรือใช้คำสั่งเองตาม[ใช้โดยไม่มี AI](https://github.com/sayam/thai-docx-skill/blob/main/docs/guide/th/command-line.md) |
| แอปบอกว่ารัน Python ไม่ได้ หรือไม่มี Python | เปิดการรันโค้ดในหน้าตั้งค่าของแอป ถ้าไม่มีให้เปิด ให้ขอแอปว่า "ช่วยเขียนเนื้อหาเป็น Markdown ให้หน่อย" แล้วสร้างไฟล์ Word เองตาม[ใช้โดยไม่มี AI](https://github.com/sayam/thai-docx-skill/blob/main/docs/guide/th/command-line.md) |
| แอปบอกว่า Python เก่าเกินไป | โปรแกรมต้องใช้ Python 3.11 ขึ้นไป แอปนี้ใช้วิธีนี้ไม่ได้ ทำแบบข้อด้านบน |
| แอปเขียนโค้ดสร้างไฟล์ Word เอง ไม่ใช้โปรแกรมในกล่อง | พิมพ์ว่า "ใช้โปรแกรมใน thai-docx ตามที่บอกไว้ในข้อความแรก" |
| ไฟล์ที่ได้มีเส้นหยักแดงใต้คำไทยทุกคำ | แอปไม่ได้ใช้โปรแกรมในกล่อง บอกแอปแบบข้อด้านบน แล้วขอไฟล์ใหม่ |
| แอปบอกว่ามี **ข้อผิดพลาดที่บรรทัด...** | ข้อความบรรทัดนั้นมีสิ่งที่โปรแกรมไม่รับ บอกแอปให้แก้บรรทัดนั้นโดยไม่เปลี่ยนความหมาย |
| แอปถามเรื่องฟอนต์ทั้งที่ไม่ได้พิมพ์ `thai-docx grill` | ตอบว่า "ใช้ค่าเริ่มต้นไปก่อน" |

ถ้ายังไม่ได้ [แจ้งที่หน้า Issues](https://github.com/sayam/thai-docx-skill/issues) บอกชื่อแอป สิ่งที่พิมพ์ และสิ่งที่เห็น
อย่าแนบเอกสารจริงของใคร
