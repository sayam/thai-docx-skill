# What was found broken, and what now holds it fixed. A fault is closed by a control, not by a
# change: each row names the test that fails if the fault comes back (tests/test_regressions.py
# holds every row to it).
#
# id        the finding's name in the review that found it
# what      the fault, as a user or an agent met it
# class     how it is held (comma-separated): C0 the wrong path no longer exists (one reader, one escape point);
#           C1 a test that fails when it returns; C2 a rule a person or a model reads — only
#           beside a C1; C3 a check after the work, by a person or in a real application — only
#           with the record it is written in
# found_by  review · outside-review · parity · planted-input · agent-run · office-app
# test      pytest node ids, `file::function`
# record    the evidence record, where a C3 row needs one

- id: R3
  what: repair wrote over the file it was given — the same path, a symbolic link to it, or a hard link
  class: C0, C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_the_input_is_never_the_output

- id: R6
  what: build wrote over its own Markdown when the output path named it
  class: C0, C1
  found_by: outside-review
  test:
    - tests/test_what_a_command_takes.py::test_the_input_is_never_the_output

- id: D4
  what: a FIFO given as an image, a profile or a package hung the command at open
  class: C0, C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_a_file_that_is_not_a_regular_file_is_refused_before_it_is_opened

- id: D-05
  what: the Markdown, and in JavaScript the package given to repair, were read whole with no ceiling
  class: C0, C1
  found_by: planted-input
  test:
    - tests/test_what_a_command_takes.py::test_a_markdown_file_past_its_ceiling_is_refused_not_read_whole
    - tests/test_build.py::test_one_picture_past_its_cap_is_refused_before_it_is_read_whole

- id: D-04
  what: an argument in another encoding stopped Python with a trace and was written as U+FFFD by JavaScript
  class: C1
  found_by: planted-input
  test:
    - tests/test_what_a_command_takes.py::test_an_argument_in_another_encoding_is_refused_the_same_way

- id: B2
  what: a lone surrogate from a profile stopped Python with a trace and was written as U+FFFD by JavaScript
  class: C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_that_is_not_a_profile_is_named_not_obeyed
    - tests/test_what_a_command_takes.py::test_a_profile_is_replaced_whole_or_not_at_all

- id: C-05
  what: a number too long for a float stopped Python with a trace; JavaScript refused it
  class: C1
  found_by: parity
  test:
    - tests/test_what_a_command_takes.py::test_a_number_too_long_for_a_float_is_refused_not_a_traceback

- id: D-02
  what: repair --font was written into the XML unread and unescaped, so a value could add an element
  class: C0, C1
  found_by: planted-input
  test:
    - tests/test_what_a_command_takes.py::test_the_font_given_to_repair_is_read_like_the_builds_and_escaped

- id: E-04
  what: repair answered a file with nothing to repair with an error that pointed at findings it did not have
  class: C1
  found_by: agent-run
  test:
    - tests/test_what_a_command_takes.py::test_a_file_with_nothing_to_repair_is_an_answer_not_an_error
    - tests/test_repair.py::test_a_clean_file_is_left_alone

- id: B4
  what: a profile name could hold what a shell reads, and grill handed it back inside a command to run
  class: C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_name_is_letters_digits_dash_and_underscore
    - tests/test_what_a_command_takes.py::test_grill_never_hands_back_a_word_a_shell_would_read

- id: B6
  what: profile save --help saved a profile named --help
  class: C1
  found_by: outside-review
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_name_is_letters_digits_dash_and_underscore

- id: B3
  what: a profile with a value of the wrong type, or JSON nested past the parser's depth, stopped Python with a trace
  class: C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_that_is_not_a_profile_is_named_not_obeyed

- id: D-07
  what: a profile keyed __proto__ was read by JavaScript and refused by Python
  class: C1
  found_by: parity
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_that_is_not_a_profile_is_named_not_obeyed

- id: D-13
  what: a profile that was not UTF-8, or said schema true or 1.0, was read by one implementation and refused by the other
  class: C1
  found_by: parity
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_that_is_not_a_profile_is_named_not_obeyed

- id: B1
  what: a profile import that failed part way left the profile it was replacing empty
  class: C0, C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_a_profile_is_replaced_whole_or_not_at_all

- id: E-15
  what: build --help and check --help answered as if --help were an option or a file
  class: C1
  found_by: agent-run
  test:
    - tests/test_what_a_command_takes.py::test_help_is_the_usage_line

- id: C-03
  what: Python printed the JSON line in the pipe's encoding and stopped with a trace where it was not UTF-8
  class: C0, C1
  found_by: parity
  test:
    - tests/test_what_a_command_takes.py::test_a_pipe_that_is_not_utf8_still_gets_the_json_line

- id: D-15
  what: a command run from a working directory that had been removed stopped with a trace
  class: C1
  found_by: planted-input
  test:
    - tests/test_what_a_command_takes.py::test_a_working_directory_that_is_gone_is_said_not_a_traceback

- id: D3
  what: a fault inside a command left a trace and no JSON line, which an agent cannot read
  class: C0, C1
  found_by: review
  test:
    - tests/test_what_a_command_takes.py::test_the_entry_answers_before_any_command_runs

- id: R1
  what: repair compared only the body and the footnotes, and changed the text of a header, a footer or a comment unseen
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_the_text_of_a_header_is_compared_too
    - tests/test_repair_reads_xml_as_xml.py::test_a_part_holding_a_comment_or_cdata_is_left_as_it_came

- id: R2
  what: repair wrote a part its own checker could not read and answered ok
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_repair_that_breaks_the_package_writes_nothing_and_is_this_versions_fault

- id: R4
  what: JavaScript repair stopped on a picture in the package, read as text
  class: C0, C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_picture_is_left_byte_for_byte_whatever_its_bytes_spell

- id: R5
  what: Python repair could edit a picture whose bytes spelled <w:noProof/>
  class: C0, C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_picture_is_left_byte_for_byte_whatever_its_bytes_spell

- id: D2
  what: putting properties in order copied the whole part at every element — twenty seconds for nine kilobytes
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_putting_properties_in_order_takes_time_in_proportion_to_the_part

- id: B-01
  what: a run cut at the script lost xml:space, so the space between Thai and English was no longer text
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_cut_run_keeps_the_space_at_the_cut

- id: B-02
  what: a run holding a tab or two w:t was cut through its markup, and the whole repair refused with a false reason
  class: C0, C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_run_with_a_tab_is_marked_whole_not_cut_through_its_markup

- id: A-01
  what: an invisible character inside a Thai word was cut out as a run of its own
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_an_invisible_character_inside_a_thai_word_is_not_a_place_to_cut

- id: D-06
  what: a > inside an attribute, or an empty run written <w:r />, ended a tag in the middle and stopped repair
  class: C0, C1
  found_by: planted-input
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_greater_than_inside_an_attribute_is_not_the_end_of_the_tag
    - tests/test_repair_reads_xml_as_xml.py::test_an_empty_run_is_passed_over

- id: D-08
  what: a size in single quotes got a twin with no value
  class: C1
  found_by: planted-input
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_single_quoted_size_gets_its_twin_with_the_same_value

- id: D-10
  what: a part under another prefix, or with w bound elsewhere, was half repaired or edited where the checker saw nothing
  class: C1
  found_by: planted-input
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_part_under_another_prefix_is_refused_not_half_repaired

- id: B-12
  what: a part written under the prefix ns0 was not repaired, with a reason that did not say why
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_part_under_another_prefix_is_refused_not_half_repaired

- id: F-01
  what: the guard that refuses a repair changing the text was never exercised by a test
  class: C1
  found_by: review
  test:
    - tests/test_repair_reads_xml_as_xml.py::test_a_repair_that_changes_a_character_writes_nothing

- id: D-01
  what: a part in UTF-16 with no byte-order mark decoded as UTF-8, passed the DOCTYPE refusal, and had its entities expanded by the parser
  class: C0, C1
  found_by: planted-input
  test:
    - tests/test_check_holds_its_limits.py::test_a_utf16_part_is_refused_not_parsed

- id: D-03
  what: a run's properties nested 500 deep stopped check with a trace, at a depth that differed by runtime
  class: C0, C1
  found_by: planted-input
  test:
    - tests/test_check_holds_its_limits.py::test_a_run_nested_past_any_stack_is_read_not_a_trace

- id: F-02
  what: the 64 MiB total cap was held by no test at its value; its only test patched the constant
  class: C1
  found_by: review
  test:
    - tests/test_check_holds_its_limits.py::test_the_size_caps_hold_at_their_values
    - tests/test_check_holds_its_limits.py::test_each_limit_is_the_value_both_implementations_state

- id: D-09
  what: the part cap and the symbolic-link cap could be raised a hundredfold with every test green
  class: C1
  found_by: review
  test:
    - tests/test_check_holds_its_limits.py::test_the_size_caps_hold_at_their_values
    - tests/test_check_holds_its_limits.py::test_forty_links_are_followed_and_the_forty_first_is_not
    - tests/test_check_holds_its_limits.py::test_each_limit_is_the_value_both_implementations_state

- id: D-09a
  what: a chain of forty-one symbolic links stopped the walk inside the directory, and the OS then followed the last one out of it — a picture outside was embedded
  class: C1
  found_by: planted-input
  test:
    - tests/test_check_holds_its_limits.py::test_forty_links_are_followed_and_the_forty_first_is_not

- id: B5
  what: a caption label holding & or < broke the field that carried it, and one holding a quote cut the field's own string
  class: C0, C1
  found_by: outside-review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_label_is_escaped_in_its_field_and_one_a_field_would_misread_is_refused

- id: B-06
  what: a caption label with a space under --auto-numbering wrote a SEQ field Word cannot name, silently
  class: C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_label_with_a_space_is_refused_only_where_word_counts

- id: B8
  what: a link with any scheme was written as an external target
  class: C0, C1
  found_by: outside-review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_link_leads_only_to_http_https_or_mailto

- id: B-07
  what: a link's target was written raw, a space or Thai in it unencoded
  class: C0, C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_link_target_is_a_uri_and_its_text_is_unchanged

- id: D1
  what: every unclosed link read to the end of the text, so eight thousand took most of a minute
  class: C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_two_thousand_open_links_are_read_in_a_moment

- id: C-02
  what: a list numbered from 0 was renumbered from 1, silently
  class: C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_list_numbered_from_zero_starts_at_zero

- id: B-03
  what: five invisible characters were refused and the rest of their kind passed into the file and the checker's verdict
  class: C0, C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_every_format_character_and_noncharacter_is_refused_by_name
    - tests/test_build_writes_what_it_was_given.py::test_the_checker_names_a_format_character_in_a_file_it_did_not_write

- id: C-11
  what: the parser said it refused noncharacters and refused two
  class: C0, C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_every_format_character_and_noncharacter_is_refused_by_name

- id: C-04
  what: a tag name in U+017F was an HTML block to Python and text to JavaScript
  class: C1
  found_by: parity
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_tag_name_is_ascii_in_both

- id: C-06
  what: an allowed tag alone on its line was refused with a message that named it as allowed
  class: C1
  found_by: review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_an_allowed_tag_alone_on_its_line_is_refused_with_the_reason

- id: C-07
  what: a small float was reported as 1e-05 by one implementation and 0.00001 by the other
  class: C1
  found_by: parity
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_small_float_is_reported_as_python_writes_it

- id: D5
  what: a picture that declared a vast size got an extent past the format, or one of nothing
  class: C1
  found_by: planted-input
  test:
    - tests/test_build_writes_what_it_was_given.py::test_a_picture_is_fitted_to_the_page_and_never_to_nothing

- id: B7
  what: grill split words at U+001C, U+0085 and U+FEFF in one implementation and not the other
  class: C0, C1
  found_by: outside-review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_grill_reads_the_same_words_in_both

- id: E-08
  what: a from or save to later in the message than grill read was dropped without a word
  class: C1
  found_by: agent-run
  test:
    - tests/test_build_writes_what_it_was_given.py::test_grill_says_what_it_did_not_read

- id: E-16
  what: one Thai word in an English request asked all nine questions in Thai
  class: C1
  found_by: agent-run
  test:
    - tests/test_build_writes_what_it_was_given.py::test_grill_asks_in_the_language_most_of_the_words_are_in

- id: GROK-04
  what: the math warning named version 0.1
  class: C1
  found_by: outside-review
  test:
    - tests/test_build_writes_what_it_was_given.py::test_math_is_said_to_be_kept_as_it_was_written_without_a_version

- id: S1
  what: actions/checkout given a bare tag name builds a branch of the same name in its place
  class: C0, C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_the_release_checks_out_the_tag_as_a_tag_and_proves_it

- id: S2
  what: the attestation was verified against the repository only, so one made by another workflow or ref would pass
  class: C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_every_way_to_verify_a_release_names_the_workflow_and_the_tag

- id: F-03
  what: release-check ran the suite but not the lints or the coverage floor its gate names
  class: C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_the_tag_passes_the_gates_a_pull_request_passes

- id: F-05
  what: CITATION.cff's version and date were checked by nothing
  class: C1
  found_by: review
  test:
    - tests/test_package_skill.py::test_the_citation_names_the_release_the_changelog_names

- id: F-09
  what: the install guides' verify command named neither the workflow nor the ref
  class: C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_every_way_to_verify_a_release_names_the_workflow_and_the_tag

- id: F-10
  what: CI ran only the oldest Python and Node the docs promise
  class: C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_the_suite_runs_on_the_oldest_and_the_newest_runtimes_promised

- id: F-11
  what: tools/preflight.py imported YAML, and the requirements installed none
  class: C1
  found_by: review
  test:
    - tests/test_release_is_bound_to_its_tag.py::test_what_the_tools_import_the_requirements_pin

- id: A-04
  what: tools/oracle_set.py --help made a directory named --help and stopped with a trace
  class: C1
  found_by: review
  test:
    - tests/test_oracle_set.py::test_a_question_is_answered_with_the_usage_and_nothing_is_made

- id: PR-53
  what: no checklist item asked whether two level-1 headings, one Thai and one not, are centred alike
  class: C1
  found_by: office-app
  test:
    - tests/test_oracle_set.py::test_the_checklist_asks_whether_two_headings_match
